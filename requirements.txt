# Bitbucket + Jira Dev Skill — Python Dependencies
# Install with: pip install -r requirements.txt --break-system-packages

# Core UI
streamlit>=1.28.0

# HTTP client (Jira API, Bitbucket REST API)
requests>=2.28.0

# .gitignore-aware file walking
pathspec>=0.11.0

# Optional: code formatting helpers (auto-detected, not imported directly)
# black>=23.0.0
# isort>=5.0.0
